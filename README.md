Vote for Pizza
==============

An app for voting for things.

Depends on scala Play and my SingleTransferableVote package available at https://github.com/MatMoore/SingleTransferableVote

To run the dev server, first check out SingleTransferableVote and run `sbt package-local`

Now run `./activator` from this project.
